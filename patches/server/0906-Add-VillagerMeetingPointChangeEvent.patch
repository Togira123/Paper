From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Togira <70365614+Togira123@users.noreply.github.com>
Date: Sat, 4 Jun 2022 17:34:35 +0200
Subject: [PATCH] Add VillagerMeetingPointChangeEvent


diff --git a/src/main/java/net/minecraft/world/entity/ai/behavior/AcquirePoi.java b/src/main/java/net/minecraft/world/entity/ai/behavior/AcquirePoi.java
index 401c6b606851c2613c716fecbdb2d512b92e37f8..e509883a9324ed8a131291709f50d23443555675 100644
--- a/src/main/java/net/minecraft/world/entity/ai/behavior/AcquirePoi.java
+++ b/src/main/java/net/minecraft/world/entity/ai/behavior/AcquirePoi.java
@@ -67,7 +67,7 @@ public class AcquirePoi extends Behavior<PathfinderMob> {
 
     @Override
     protected void start(ServerLevel world, PathfinderMob entity, long time) {
-        // Paper start - VillagerHomeChangeEvent
+        // Paper start - VillagerHomeChangeEvent & VillagerMeetingPointChangeEvent
         if (this.memoryToAcquire.equals(MemoryModuleType.HOME)) {
             org.bukkit.entity.Entity bukkitEntity = entity.getBukkitEntity();
             if (bukkitEntity instanceof org.bukkit.entity.Villager) {
@@ -76,8 +76,16 @@ public class AcquirePoi extends Behavior<PathfinderMob> {
                     return;
                 }
             }
+        } else if (this.memoryToAcquire.equals(MemoryModuleType.MEETING_POINT)) {
+            org.bukkit.entity.Entity bukkitEntity = entity.getBukkitEntity();
+            if (bukkitEntity instanceof org.bukkit.entity.Villager) {
+                boolean cancelled = new io.papermc.paper.event.entity.VillagerMeetingPointChangeEvent((org.bukkit.entity.Villager) bukkitEntity).callEvent();
+                if (cancelled) {
+                    return;
+                }
+            }
         }
-        // Paper end - VillagerHomeChangeEvent
+        // Paper end - VillagerHomeChangeEvent & VillagerMeetingPointChangeEvent
         this.nextScheduledStart = time + 20L + (long)world.getRandom().nextInt(20);
         PoiManager poiManager = world.getPoiManager();
         this.batchCache.long2ObjectEntrySet().removeIf((entry) -> {
